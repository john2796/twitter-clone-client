const initialState = {
  data: [],
  error: null,
  text: "",
  isLoading: true
};

export default function commentBoxReducers(state = initialState, action) {
  // switch (action.type) {
  // case 'ACTION_TYPE':
  //   return code
  // default:
  //     return state
  // }
  return state;
}
types;
